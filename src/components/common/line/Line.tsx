import React, {FC} from "react"
import {ReturnComponentType} from "types"
import style from "./Line.module.scss"

export const Line: FC = (): ReturnComponentType => <div className={style.line}></div>
